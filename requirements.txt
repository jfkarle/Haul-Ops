streamlit==1.34.0  # Or a slightly older recent version
streamlit-gsheets==0.2.1 # Current latest as of now
gspread==5.12.4       # Current latest as of now
pandas==2.0.3         # A widely used version
